# PostgreSQL connection for Inventory & Catalog service
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=inventorydb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# JWT secret used to validate bearer tokens from Authentication service
# Must match the issuer side secret
JWT_SECRET=replace-with-the-shared-jwt-secret
